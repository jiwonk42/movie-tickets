//BUSINESS LOGIC
//Create constructor
function Choice(movie,time,age) = {
  this.movie = movie;
  this.time = time;
  this.age = age;
}

//USER INTERFACE
$(document).ready(function();
  $("form").submit(function(event){
    event.preventDefault();

  });
});
